name: e2e_apply
on: [push]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4v4
      - uses: actions/setup-python@v5v5
        with:
          python-version: '3.11'
      - run: echo "E2E sandbox"
